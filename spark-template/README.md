* Supports:
  * Spark 4.0
  * Scala 2.13.x

* Structure
  * script module for the driver class only
  * library module for the remaining code.
  * library module can be package in thin jar or fat jar, depending on deployment env. 